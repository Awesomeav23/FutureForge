# Copy to .env and fill in, or just export these in your shell.
# Without a key FutureForge runs in offline mode and still works end to end.
OPENAI_API_KEY=sk-your-key-here
FUTUREFORGE_MODEL=gpt-4o-mini
